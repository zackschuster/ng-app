image: alpine/edge
packages:
  - npm
sources:
  - https://git.sr.ht/~ledge/ng-app
tasks:
  - install: |
      cd ng-app
      npx npm@7 install
  - path: |
      npx -p node@10 /bin/sh -c 'echo $PATH'
  - test: |
      cd ng-app
      npx -p node@10 npm test
      npx -p node@12 npm test
      npx -p node@14 npm test
      npx -p node@15 npm test
