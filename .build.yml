image: alpine/edge
packages:
  - nodejs-current
  - npm
sources:
  - https://git.sr.ht/~ledge/ng-app
tasks:
  - install: |
      cd ng-app
      npx npm@7 install
  - test: |
      cd ng-app
      npm test
